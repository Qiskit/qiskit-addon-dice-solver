---
fixes:
  - | 
    Users should no longer flip the orbital occupancies output from :func:`qiskit_addon_dice_solver.solve_fermion` and :func:`qiskit_addon_dice_solver.solve_hci`; they will be output in the correct order (the same order as the bitstrings).
